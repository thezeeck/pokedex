import styled from "styled-components";

export const PokemonListContainer = styled.ul`
  padding: 0 .5rem;
  list-style: none;
  display: flex;
  flex-wrap: wrap;
  margin: 0 auto 1rem;
  max-width: 1200px;
`